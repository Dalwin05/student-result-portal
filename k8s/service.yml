apiVersion: v1
kind: Service
metadata:
  name: student-portal-service
  namespace: student-portal
  labels:
    app: student-portal
    project: 24CS2018-DevOps
spec:
  type: LoadBalancer
  selector:
    app: student-portal
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
